<!-- El control corre en segundo plano (icono de bandeja): no abre ventana al
     iniciar y cerrar la de diagnóstico no termina el proceso. -->
<Application x:Class="TrueCentralVms.WebControl.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <!-- Paleta CLR TrueCentral (misma que el cliente y el watchdog) -->
        <SolidColorBrush x:Key="BgBrush" Color="#0F141A"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#171F28"/>
        <SolidColorBrush x:Key="Panel2Brush" Color="#1E2833"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2B3947"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E6EDF3"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#8A9AA9"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#2F81F7"/>
        <SolidColorBrush x:Key="Accent2Brush" Color="#1F6FEB"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#E5534B"/>
        <SolidColorBrush x:Key="OkBrush" Color="#3FB950"/>
        <SolidColorBrush x:Key="WarnBrush" Color="#D29922"/>

        <Style TargetType="Window" x:Key="DarkWindow">
            <Setter Property="Background" Value="{StaticResource BgBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,5"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Panel2Brush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Accent2Brush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Accent2Brush}"/>
        </Style>
    </Application.Resources>
</Application>
