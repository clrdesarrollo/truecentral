{
  // Ajustes propios de ESTE equipo. Este archivo tiene prioridad sobre
  // appsettings.json y appsettings.Production.json, y las actualizaciones
  // del sistema NUNCA lo sobreescriben.
  //
  // Ejemplos (quite las barras // para activar y reinicie el servicio):
  //
  // "Urls": "http://*:8080",                 // use "*" (IPv4+IPv6), no "0.0.0.0"
  // "Streaming": { "ServerPort": 8080,       // el mismo puerto que "Urls"
  //                "PublicHost": "vms.miempresa.cl" },   // host que ven los clientes en las URL RTSP
  // "Database": { "PgPort": 25491 },         // solo antes de crear la base (el puerto vive en el clúster)
  // "Alarms": { "Receiver": { "Port": 5091 } },
  // "Supervisor": { "CheckSeconds": 10, "MaxRestartAttempts": 5 },
  // "Hikvision": { "SdkLogDirectory": "%ProgramData%\\CLRTrueCentralVMS\\logs\\hcnetsdk" }
}
